mybatis:
  mapper-locations: classpath*:mapping/*.xml
spring:
  datasource:
    url: jdbc:mysql://ip:3306/databasename?useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true
    username: username
    password: password
    driver-class-name: com.mysql.jdbc.Driver
server:
  port: 8080
